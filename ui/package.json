{
  "private": true,
  "scripts": {
    "test": "ace pom && lerna run test",
    "bootstrap": "lerna bootstrap",
    "build": "lerna run build && ace gen-feature"
  },
  "devDependencies": {
    "ace": "*",
    "lerna": "2.8.0"
  },
  "workspaces": [
    "packages/*"
  ]
}
