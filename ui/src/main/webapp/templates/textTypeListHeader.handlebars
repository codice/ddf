<div class='{{id}} metatype control-group'>
    <label class='control-label' for='{{id}}'>{{name}}</label>
            <div class="btn-group">
                <button type="button" style="width:25px;height:25px" class="small btn-secondary plus-button">+</button>
        </div>
        {{#if description}}
            <a href='#' class='description' data-title='{{id}}'>
                <i class='glyphicon glyphicon-question-sign'></i>
            </a>
        {{/if}}
        
        <div id='{{id}}'>
        
        </div>
        
</div>