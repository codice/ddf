<!DOCTYPE html>
<html lang="en">
  <head>
    <link href="{{baseUrl}}/search/simple/lib/bootstrap-2.3.1/css/bootstrap.min.css" rel="stylesheet">
  </head>

  <body>
    <div class="container">
      <div class="hero-unit">
    	<div class="row">
    		<div class="span2">
    		{{#logo}}
    			<img src="{{logo}}"/>
    		{{else}}
    			<img src="{{baseUrl}}/search/simple/images/ddf_globe.png"/>
    		{{/logo}}
    		</div>
    		<div class="span8">
        		<h1>{{productName}}</h1>
        		<p>{{description}}</p>
        		<p><a href="{{baseUrl}}/search" class="btn btn-primary btn-large">Search {{productName}} &raquo;</a></p>
        	</div>
        	
        </div>
        <div id="contactInfo" class="row">
        	<div class="span2 offset8">
        		<small>
        			<address>
        				<strong>Contact Information:</strong><br/>
        				<span id="email"><a href="mailto:{{contact}}">{{contact}}</a><br/></span>
        				<span id="externalUrl"><a href="{{webSite}}">{{webSite}}</a><br/></span>
        			</address>
        		</small>
        	</div>
        </div>
      </div>
    </div>
  </body>
</html>
