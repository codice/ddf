<div class="metacard-category">
  <h3 class="category-title">{{ title }}<button class="category-control" onclick="toggleCategory(this.parentElement.parentElement)"><i class="up"></i></button></h3>
  <table class="category-table">
    <tr>
      <th class="attribute-name">Property</th>
      <th class="attribute-value">Value</th>
    </tr>
    {{#each attributeMappings}}
      <tr class="metacard-attribute">
        <td class="attribute-name">{{ @key }}</td>

        {{#isEmptyValue}}
          <td class="attribute-value empty-attribute">{{ value }}</td>
        {{/isEmptyValue}}

        {{#isMediaValue}}
          <td class="attribute-value media-attribute">
            <img src="data:image/png;base64, {{ value }}"/>
          </td>
        {{/isMediaValue}}

        {{#isBasicValue}}
          <td class="attribute-value">{{ value }}</td>
        {{/isBasicValue}}

      </tr>
    {{/each}}
  </table>
</div>
